# Calculate comment=#
# to compile with the gcc 4.9.1 and -Os
#?os_assemble_arch_machine!=x86_64#
CFLAGS="-O3 -pipe -march=i686"
#os_arch_machine#
#?os_assemble_arch_machine==x86_64#
CFLAGS="-O3 -pipe -march=x86_64"
#os_arch_machine#
CXXFLAGS="${CFLAGS}"
